# O @google/model-viewer (AR) declara peer three@^0.183, e o projeto usa
# three@0.185. As versões são compatíveis na prática; legacy-peer-deps faz o
# npm (inclusive o do Vercel) aceitar o conflito em vez de abortar o install.
legacy-peer-deps=true
